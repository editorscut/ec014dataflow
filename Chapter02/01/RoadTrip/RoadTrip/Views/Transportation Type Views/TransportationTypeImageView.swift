import SwiftUI

struct TransportationTypeImageView: View {
  var body: some View {
    let _ = print(self)
    Image(systemName: "globe")
      .imageScale(.large)
      .foregroundStyle(.tint)
  }
}

#Preview {
  TransportationTypeImageView()
}
