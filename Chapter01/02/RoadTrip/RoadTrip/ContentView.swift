import SwiftUI

struct ContentView: View {
  var body: some View {
    let _ = print(Body.self)
    VStack {
      Image(systemName: "globe")
//        .imageScale(.large)
//        .foregroundStyle(.tint)
      Text("Hello, world!")
    }
//    .padding()
  }
}

#Preview {
  ContentView()
}
